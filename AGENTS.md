# 项目协作规则

## 新任务交接摘要

每次需要整理当前工作、进行上下文压缩或交接给后续任务时，输出“新任务交接摘要”，且不超过 12 条。

摘要必须包含：目标、已修改文件、已验证结果、未解决问题、关键命令、下一步。

不要复述完整过程，不要粘贴完整日志。

## 功能实现边界

仅实现功能需求本身，禁止进行备份，容灾，安全性等过度设计。

## 部署授权边界

- 默认只在本地修改、测试和构建。
- 未经用户当次明确授权“部署到生产”或“投产”，禁止连接生产服务器、推送生产部署或重建生产容器。
- “直接实施”“继续”“验收”等表述只授权本地实施与验证，不等同于生产部署授权。

## 前端实现顺序

- 新增或调整前端功能时，优先完成并验证 PC 端实现。
- PC 端功能与交互确认后，再单独进行手机端适配。
- 不进行 iPad 或平板端专项适配，除非用户后续明确提出。

## 前端页面设计原则

- 遵守“单页单任务”原则：一个路由只承载一个主要任务；复杂功能必须拆成子路由，不在同一页面纵向堆叠多个完整模块。
- 页面标题、分区标题、导航名称和操作标题统一使用中文；禁止在中文标题上方或下方添加纯装饰性的英文眉题。
- 产品名、技术名词、代码、接口路径和真实业务数据可保留英文，不受上一条限制。
